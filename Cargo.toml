[workspace]
members = ["src-tauri"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["ocg-manager"]
license = "MIT"
repository = ""

[profile.release]
opt-level = 3
lto = true
strip = true
panic = "abort"
