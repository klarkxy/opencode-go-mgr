# 2.2.0 本地发布候选评审

[English](README.md)

本次从 `39e85125` 开始评审。修复已提交至候选分支，草稿 PR #57 与三平台候选 CI 已获授权并启动；未打标签、正式发布或部署。

## 已修复

- 无鉴权用户定义供应商不再被要求提供 Key，也不会透传客户端凭据。OpenCode 专用身份头仅发送到 Go/Zen，并保留适用的客户端显式值。
- 目录中新增的未知 Zen Free 模型默认使用 Chat，原始名称与去掉 `-free` 的别名均能转发；未保存名称仍在本地拒绝，显式关闭的协议仍保持关闭。
- 账号字段与 Ollama 计费档位在同一事务中提交。注入计费写入失败后，原字段、Key 密文与档位均不变。
- Host 使用密钥打开数据库时，在迁移前检查已有账号密文，当前版本数据库也不再跳过检查。解密失败不会改写数据。
- 桌面初始化在取得单实例所有权后进行。第二次启动不会创建另一份数据，监听端口失败会终止启动。
- Unix CPA 使用现有可执行文件的临时监督模式与生命周期管道，在有界 TERM/KILL 清理完成前保持进程组身份。宿主被强制结束、CPA 提前退出或后代保留输出管道时，不再误用旧进程组编号或无限等待日志线程。
- 前端缓存拒绝过期请求，退出登录使待完成的 Key 请求失效，同进程版本号不会倒退；业务冲突保留真实错误，未修改的设置数组采用远端更新，刷新主 Key 会同步页面显示与复制值。
- 补齐缺失的繁体中文标签及多账号探测提示，修正文档中的旧导航和 Zen 测试预期。最低 Rust 版本对齐锁定依赖的 1.88，并采用编译器给出的等价语法简化，通过严格警告检查。

Grok、Kimi 承担有范围约束的评审与修复；Unix 进程所有权另有独立方案检查、实施和最终源码检查。主智能体复核实际修改及验证证据。

## 验证结果

| 范围 | 结果 |
| --- | --- |
| 前端测试 | 229 项通过 |
| 前端类型检查及生产构建 | 通过 |
| 发布工具测试 | 77 项通过 |
| Dashboard V3 生成合约 | 通过，无 schema 变更 |
| 设计检查、Rust 格式及差异空白检查 | 通过 |
| Windows Rust 工作区验证 | 修正后重跑网关套件，各目标均通过 |
| 最终 Windows core 单元测试 | 762 通过、2 忽略 |
| 最终网关集成测试 | 86 项通过 |
| Linux 工作区，不含 Tauri 桌面 | Rust 1.88 下 51 个目标，1,446 通过、3 忽略 |
| Linux CPA 生命周期专项 | 14 项通过，含宿主 SIGKILL 与后代清理 |
| Windows 全工作区严格 Clippy | 全目标通过，警告视为错误 |
| 版本与本地发布预检 | 2.2.0 通过 |
| Windows 安装包及 CLI 包构建 | 通过，为本地未签名产物 |
| 最终包校验值 | `release/` 中两个文件均已核验 |
| 从最终压缩包解出的 CLI | 使用本地模拟上游，控制台、账号增删改、CAS 冲突、鉴权、模型映射、非流式和 SSE 均通过 |
| 最终桌面可执行文件 | 重复启动在创建第二份数据前退出，原网关保持运行；端口冲突终止启动；重启及版本验证通过 |
| 实际控制台交互 | 最终仪表盘和账号页正常显示，检查了 CPA 模型目录空状态，Key 保持遮蔽 |

Windows 全量运行发现了免费模型别名的诊断入口问题，修正后的整组网关测试已重跑通过；最终 core 套件也重新运行。编译器等价语法调整通过格式与严格 Clippy 检查。Linux 完成了整合后的全部非桌面套件。

## 产物

| `release/` 内文件 | SHA-256 |
| --- | --- |
| `ocg-manager_2.2.0_windows-x64-setup.exe` | `cc832e436a794fdce9c955229f963fbcbb501794aba6be63c8836d7c278e762e` |
| `ocg-manager-cli_2.2.0_windows-x64.zip` | `f46d48e4104729e992113ed346318e70a7a7a74fc2d5cbb11109cee1996d0eff` |

## 证据边界与评审裁定

- 本次未执行 macOS 实机运行、安装器安装/卸载、签名更新分发或真实供应商/OAuth 会话。签名平台矩阵与各平台冒烟检查仍属于获得授权后的发布流程。
- 既有 XOR Key 混淆并非认证加密。解密检查不能识别所有恰好产生合法 UTF-8 的错误密钥；本次未引入加密格式迁移。
- Applications 导航此前已明确移除，不予恢复。无鉴权供应商保留文档规定的先删除账号、再删除供应商的非级联流程；删除唯一账号会结束路由，但空供应商仍可删除。重新创建供应商会重建其无鉴权单例。
- 未加入统一的短请求超时，以免打断合法的长时间管理或推理操作。关于 CAS 不会自动更新、整套繁体中文缺失、网关日志翻页的评审推断，经追踪当前实现后排除。
- 依赖漏洞查询因本机 TLS/代理错误未完成，本报告不宣称已完成依赖漏洞审计。

验收使用独立测试数据。临时桌面进程已停止，应用的两项开机启动注册表值已核对并恢复。

## 下一道验收门槛

完整发布就绪尚未证明。下一步需提交候选修改、推送独立候选分支并创建草稿 PR 运行 Quality，再手动触发 `release.yml` 的 `target=all`。已检查工作流：手动候选执行不创建或发布 GitHub Release，不提供 updater 签名密钥；它覆盖三个平台构建、Windows 安装检查、macOS 挂载启动和 Linux 虚拟显示启动。这些 CI 检查仍不能替代上文的签名更新及人工实机检查。候选分支与草稿 PR [#57](https://github.com/klarkxy/opencode-go-mgr/pull/57) 已获授权并创建。Unix 候选额外运行 CPA 生命周期专项测试，以覆盖 macOS 上的进程清理行为；CI 结果待补齐。
